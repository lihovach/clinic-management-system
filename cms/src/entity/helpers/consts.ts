export enum PetType {
  DOG = "dog",
  CAT = "cat",
  BIRD = "bird",
  OTHER = "other",
}

export enum Gender {
  MALE = "male",
  FEMALE = "female",
}

export enum AppointmentStatus {
  PENDING = "pending",
  DONE = "done",
  REJECTED = "rejected",
}
